apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: questions-ingress
spec:
  rules:
    - host: api-questions-dev.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: questions-service
                port:
                  name: http
