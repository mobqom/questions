apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: apps-dev

resources:
  - ../base
  - namespace.yaml
  - configmap.yaml
  - secret.yaml

patches:
  - path: patch-deployment.yaml
  - path: patch-ingress.yaml

images:
  - name: ghcr.io/mobqom/questions
    newTag: dev-latest
