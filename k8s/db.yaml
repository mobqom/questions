apiVersion: apps/v1
kind: Deployment
metadata:
  name: questions-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: questions-db
  template:
    metadata:
      labels:
        app: questions-db
    spec:
      containers:
        - name: db
          image: postgres:17-alpine
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: questions-db-secret
                  key: db_user
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: questions-db-secret
                  key: db_pass
            - name: POSTGRES_DB
              valueFrom:
                configMapKeyRef:
                  name: questions-config
                  key: db_name
          ports:
            - containerPort: 5432
          readinessProbe:
            tcpSocket:
              port: 5432
            initialDelaySeconds: 3
            periodSeconds: 2
            timeoutSeconds: 1
            failureThreshold: 15
          livenessProbe:
            tcpSocket:
              port: 5432
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 6
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: questions-db-service
spec:
  selector:
    app: questions-db
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP